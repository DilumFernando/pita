defaults:
  - experiment: alps_1d_2gmm
  - _self_

seed: 40
device: auto

eval:
  sampler: nets
  checkpoint_name: best_weighted_w2
  num_samples: 1000
  true_sample_count: null
  dt: 0.01
  steps: 250
  epsilon: 1.0
  plot_every: 0

hydra:
  run:
    dir: .
  output_subdir: null
  job:
    chdir: false
